{
  "name": "event-management-system",
  "packageManager": "bun@1.1.0",
  "private": true,
  "dependencies": {},
  "scripts": {
    "build": "turbo run build",
    "lint": "turbo run lint",
    "dev": "turbo run dev"
  },
  "devDependencies": {
    "turbo": "^2.7.5"
  },
  "workspaces": [
    "client",
    "server"
  ]
}
